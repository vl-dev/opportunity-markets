/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { OPPORTUNITY_MARKET_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const CANCEL_UPDATE_AUTHORITY_CHANGE_DISCRIMINATOR = new Uint8Array([
  193, 127, 213, 94, 136, 199, 133, 229,
]);

export function getCancelUpdateAuthorityChangeDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CANCEL_UPDATE_AUTHORITY_CHANGE_DISCRIMINATOR
  );
}

export type CancelUpdateAuthorityChangeInstruction<
  TProgram extends string = typeof OPPORTUNITY_MARKET_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountPlatformConfig extends string | AccountMeta<string> = string,
  TAccountTimelockedChange extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountPlatformConfig extends string
        ? ReadonlyAccount<TAccountPlatformConfig>
        : TAccountPlatformConfig,
      TAccountTimelockedChange extends string
        ? WritableAccount<TAccountTimelockedChange>
        : TAccountTimelockedChange,
      ...TRemainingAccounts,
    ]
  >;

export type CancelUpdateAuthorityChangeInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CancelUpdateAuthorityChangeInstructionDataArgs = {};

export function getCancelUpdateAuthorityChangeInstructionDataEncoder(): FixedSizeEncoder<CancelUpdateAuthorityChangeInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: CANCEL_UPDATE_AUTHORITY_CHANGE_DISCRIMINATOR,
    })
  );
}

export function getCancelUpdateAuthorityChangeInstructionDataDecoder(): FixedSizeDecoder<CancelUpdateAuthorityChangeInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCancelUpdateAuthorityChangeInstructionDataCodec(): FixedSizeCodec<
  CancelUpdateAuthorityChangeInstructionDataArgs,
  CancelUpdateAuthorityChangeInstructionData
> {
  return combineCodec(
    getCancelUpdateAuthorityChangeInstructionDataEncoder(),
    getCancelUpdateAuthorityChangeInstructionDataDecoder()
  );
}

export type CancelUpdateAuthorityChangeAsyncInput<
  TAccountSigner extends string = string,
  TAccountPlatformConfig extends string = string,
  TAccountTimelockedChange extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  platformConfig: Address<TAccountPlatformConfig>;
  timelockedChange?: Address<TAccountTimelockedChange>;
};

export async function getCancelUpdateAuthorityChangeInstructionAsync<
  TAccountSigner extends string,
  TAccountPlatformConfig extends string,
  TAccountTimelockedChange extends string,
  TProgramAddress extends Address = typeof OPPORTUNITY_MARKET_PROGRAM_ADDRESS,
>(
  input: CancelUpdateAuthorityChangeAsyncInput<
    TAccountSigner,
    TAccountPlatformConfig,
    TAccountTimelockedChange
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  CancelUpdateAuthorityChangeInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountPlatformConfig,
    TAccountTimelockedChange
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? OPPORTUNITY_MARKET_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    platformConfig: { value: input.platformConfig ?? null, isWritable: false },
    timelockedChange: {
      value: input.timelockedChange ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.timelockedChange.value) {
    accounts.timelockedChange.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            116, 105, 109, 101, 108, 111, 99, 107, 101, 100, 95, 99, 104, 97,
            110, 103, 101,
          ])
        ),
        getBytesEncoder().encode(
          new Uint8Array([
            117, 112, 100, 97, 116, 101, 95, 97, 117, 116, 104, 111, 114, 105,
            116, 121,
          ])
        ),
        getAddressEncoder().encode(
          expectAddress(accounts.platformConfig.value)
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.platformConfig),
      getAccountMeta(accounts.timelockedChange),
    ],
    data: getCancelUpdateAuthorityChangeInstructionDataEncoder().encode({}),
    programAddress,
  } as CancelUpdateAuthorityChangeInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountPlatformConfig,
    TAccountTimelockedChange
  >);
}

export type CancelUpdateAuthorityChangeInput<
  TAccountSigner extends string = string,
  TAccountPlatformConfig extends string = string,
  TAccountTimelockedChange extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  platformConfig: Address<TAccountPlatformConfig>;
  timelockedChange: Address<TAccountTimelockedChange>;
};

export function getCancelUpdateAuthorityChangeInstruction<
  TAccountSigner extends string,
  TAccountPlatformConfig extends string,
  TAccountTimelockedChange extends string,
  TProgramAddress extends Address = typeof OPPORTUNITY_MARKET_PROGRAM_ADDRESS,
>(
  input: CancelUpdateAuthorityChangeInput<
    TAccountSigner,
    TAccountPlatformConfig,
    TAccountTimelockedChange
  >,
  config?: { programAddress?: TProgramAddress }
): CancelUpdateAuthorityChangeInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountPlatformConfig,
  TAccountTimelockedChange
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? OPPORTUNITY_MARKET_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    platformConfig: { value: input.platformConfig ?? null, isWritable: false },
    timelockedChange: {
      value: input.timelockedChange ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.platformConfig),
      getAccountMeta(accounts.timelockedChange),
    ],
    data: getCancelUpdateAuthorityChangeInstructionDataEncoder().encode({}),
    programAddress,
  } as CancelUpdateAuthorityChangeInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountPlatformConfig,
    TAccountTimelockedChange
  >);
}

export type ParsedCancelUpdateAuthorityChangeInstruction<
  TProgram extends string = typeof OPPORTUNITY_MARKET_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    platformConfig: TAccountMetas[1];
    timelockedChange: TAccountMetas[2];
  };
  data: CancelUpdateAuthorityChangeInstructionData;
};

export function parseCancelUpdateAuthorityChangeInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedCancelUpdateAuthorityChangeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      platformConfig: getNextAccount(),
      timelockedChange: getNextAccount(),
    },
    data: getCancelUpdateAuthorityChangeInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
