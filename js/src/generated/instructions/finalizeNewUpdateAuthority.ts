/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { OPPORTUNITY_MARKET_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const FINALIZE_NEW_UPDATE_AUTHORITY_DISCRIMINATOR = new Uint8Array([
  46, 87, 119, 22, 184, 132, 188, 216,
]);

export function getFinalizeNewUpdateAuthorityDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    FINALIZE_NEW_UPDATE_AUTHORITY_DISCRIMINATOR
  );
}

export type FinalizeNewUpdateAuthorityInstruction<
  TProgram extends string = typeof OPPORTUNITY_MARKET_PROGRAM_ADDRESS,
  TAccountUpdateAuthority extends string | AccountMeta<string> = string,
  TAccountProposedAuthority extends string | AccountMeta<string> = string,
  TAccountPlatformConfig extends string | AccountMeta<string> = string,
  TAccountTimelockedChange extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountUpdateAuthority extends string
        ? WritableSignerAccount<TAccountUpdateAuthority> &
            AccountSignerMeta<TAccountUpdateAuthority>
        : TAccountUpdateAuthority,
      TAccountProposedAuthority extends string
        ? ReadonlySignerAccount<TAccountProposedAuthority> &
            AccountSignerMeta<TAccountProposedAuthority>
        : TAccountProposedAuthority,
      TAccountPlatformConfig extends string
        ? WritableAccount<TAccountPlatformConfig>
        : TAccountPlatformConfig,
      TAccountTimelockedChange extends string
        ? WritableAccount<TAccountTimelockedChange>
        : TAccountTimelockedChange,
      ...TRemainingAccounts,
    ]
  >;

export type FinalizeNewUpdateAuthorityInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type FinalizeNewUpdateAuthorityInstructionDataArgs = {};

export function getFinalizeNewUpdateAuthorityInstructionDataEncoder(): FixedSizeEncoder<FinalizeNewUpdateAuthorityInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: FINALIZE_NEW_UPDATE_AUTHORITY_DISCRIMINATOR,
    })
  );
}

export function getFinalizeNewUpdateAuthorityInstructionDataDecoder(): FixedSizeDecoder<FinalizeNewUpdateAuthorityInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getFinalizeNewUpdateAuthorityInstructionDataCodec(): FixedSizeCodec<
  FinalizeNewUpdateAuthorityInstructionDataArgs,
  FinalizeNewUpdateAuthorityInstructionData
> {
  return combineCodec(
    getFinalizeNewUpdateAuthorityInstructionDataEncoder(),
    getFinalizeNewUpdateAuthorityInstructionDataDecoder()
  );
}

export type FinalizeNewUpdateAuthorityAsyncInput<
  TAccountUpdateAuthority extends string = string,
  TAccountProposedAuthority extends string = string,
  TAccountPlatformConfig extends string = string,
  TAccountTimelockedChange extends string = string,
> = {
  updateAuthority: TransactionSigner<TAccountUpdateAuthority>;
  /** The proposed new authority must co-sign to prevent fat-finger mistakes. */
  proposedAuthority: TransactionSigner<TAccountProposedAuthority>;
  platformConfig: Address<TAccountPlatformConfig>;
  timelockedChange?: Address<TAccountTimelockedChange>;
};

export async function getFinalizeNewUpdateAuthorityInstructionAsync<
  TAccountUpdateAuthority extends string,
  TAccountProposedAuthority extends string,
  TAccountPlatformConfig extends string,
  TAccountTimelockedChange extends string,
  TProgramAddress extends Address = typeof OPPORTUNITY_MARKET_PROGRAM_ADDRESS,
>(
  input: FinalizeNewUpdateAuthorityAsyncInput<
    TAccountUpdateAuthority,
    TAccountProposedAuthority,
    TAccountPlatformConfig,
    TAccountTimelockedChange
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  FinalizeNewUpdateAuthorityInstruction<
    TProgramAddress,
    TAccountUpdateAuthority,
    TAccountProposedAuthority,
    TAccountPlatformConfig,
    TAccountTimelockedChange
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? OPPORTUNITY_MARKET_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    updateAuthority: { value: input.updateAuthority ?? null, isWritable: true },
    proposedAuthority: {
      value: input.proposedAuthority ?? null,
      isWritable: false,
    },
    platformConfig: { value: input.platformConfig ?? null, isWritable: true },
    timelockedChange: {
      value: input.timelockedChange ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.timelockedChange.value) {
    accounts.timelockedChange.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            116, 105, 109, 101, 108, 111, 99, 107, 101, 100, 95, 99, 104, 97,
            110, 103, 101,
          ])
        ),
        getBytesEncoder().encode(
          new Uint8Array([
            117, 112, 100, 97, 116, 101, 95, 97, 117, 116, 104, 111, 114, 105,
            116, 121,
          ])
        ),
        getAddressEncoder().encode(
          expectAddress(accounts.platformConfig.value)
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.updateAuthority),
      getAccountMeta(accounts.proposedAuthority),
      getAccountMeta(accounts.platformConfig),
      getAccountMeta(accounts.timelockedChange),
    ],
    data: getFinalizeNewUpdateAuthorityInstructionDataEncoder().encode({}),
    programAddress,
  } as FinalizeNewUpdateAuthorityInstruction<
    TProgramAddress,
    TAccountUpdateAuthority,
    TAccountProposedAuthority,
    TAccountPlatformConfig,
    TAccountTimelockedChange
  >);
}

export type FinalizeNewUpdateAuthorityInput<
  TAccountUpdateAuthority extends string = string,
  TAccountProposedAuthority extends string = string,
  TAccountPlatformConfig extends string = string,
  TAccountTimelockedChange extends string = string,
> = {
  updateAuthority: TransactionSigner<TAccountUpdateAuthority>;
  /** The proposed new authority must co-sign to prevent fat-finger mistakes. */
  proposedAuthority: TransactionSigner<TAccountProposedAuthority>;
  platformConfig: Address<TAccountPlatformConfig>;
  timelockedChange: Address<TAccountTimelockedChange>;
};

export function getFinalizeNewUpdateAuthorityInstruction<
  TAccountUpdateAuthority extends string,
  TAccountProposedAuthority extends string,
  TAccountPlatformConfig extends string,
  TAccountTimelockedChange extends string,
  TProgramAddress extends Address = typeof OPPORTUNITY_MARKET_PROGRAM_ADDRESS,
>(
  input: FinalizeNewUpdateAuthorityInput<
    TAccountUpdateAuthority,
    TAccountProposedAuthority,
    TAccountPlatformConfig,
    TAccountTimelockedChange
  >,
  config?: { programAddress?: TProgramAddress }
): FinalizeNewUpdateAuthorityInstruction<
  TProgramAddress,
  TAccountUpdateAuthority,
  TAccountProposedAuthority,
  TAccountPlatformConfig,
  TAccountTimelockedChange
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? OPPORTUNITY_MARKET_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    updateAuthority: { value: input.updateAuthority ?? null, isWritable: true },
    proposedAuthority: {
      value: input.proposedAuthority ?? null,
      isWritable: false,
    },
    platformConfig: { value: input.platformConfig ?? null, isWritable: true },
    timelockedChange: {
      value: input.timelockedChange ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.updateAuthority),
      getAccountMeta(accounts.proposedAuthority),
      getAccountMeta(accounts.platformConfig),
      getAccountMeta(accounts.timelockedChange),
    ],
    data: getFinalizeNewUpdateAuthorityInstructionDataEncoder().encode({}),
    programAddress,
  } as FinalizeNewUpdateAuthorityInstruction<
    TProgramAddress,
    TAccountUpdateAuthority,
    TAccountProposedAuthority,
    TAccountPlatformConfig,
    TAccountTimelockedChange
  >);
}

export type ParsedFinalizeNewUpdateAuthorityInstruction<
  TProgram extends string = typeof OPPORTUNITY_MARKET_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    updateAuthority: TAccountMetas[0];
    /** The proposed new authority must co-sign to prevent fat-finger mistakes. */
    proposedAuthority: TAccountMetas[1];
    platformConfig: TAccountMetas[2];
    timelockedChange: TAccountMetas[3];
  };
  data: FinalizeNewUpdateAuthorityInstructionData;
};

export function parseFinalizeNewUpdateAuthorityInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedFinalizeNewUpdateAuthorityInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      updateAuthority: getNextAccount(),
      proposedAuthority: getNextAccount(),
      platformConfig: getNextAccount(),
      timelockedChange: getNextAccount(),
    },
    data: getFinalizeNewUpdateAuthorityInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
