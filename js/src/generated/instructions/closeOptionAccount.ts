/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { OPPORTUNITY_MARKET_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const CLOSE_OPTION_ACCOUNT_DISCRIMINATOR = new Uint8Array([
  46, 121, 107, 240, 123, 190, 229, 1,
]);

export function getCloseOptionAccountDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLOSE_OPTION_ACCOUNT_DISCRIMINATOR
  );
}

export type CloseOptionAccountInstruction<
  TProgram extends string = typeof OPPORTUNITY_MARKET_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountCreator extends string | AccountMeta<string> = string,
  TAccountMarket extends string | AccountMeta<string> = string,
  TAccountOption extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountCreator extends string
        ? WritableAccount<TAccountCreator>
        : TAccountCreator,
      TAccountMarket extends string
        ? WritableAccount<TAccountMarket>
        : TAccountMarket,
      TAccountOption extends string
        ? WritableAccount<TAccountOption>
        : TAccountOption,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CloseOptionAccountInstructionData = {
  discriminator: ReadonlyUint8Array;
  optionId: bigint;
};

export type CloseOptionAccountInstructionDataArgs = {
  optionId: number | bigint;
};

export function getCloseOptionAccountInstructionDataEncoder(): FixedSizeEncoder<CloseOptionAccountInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['optionId', getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CLOSE_OPTION_ACCOUNT_DISCRIMINATOR })
  );
}

export function getCloseOptionAccountInstructionDataDecoder(): FixedSizeDecoder<CloseOptionAccountInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['optionId', getU64Decoder()],
  ]);
}

export function getCloseOptionAccountInstructionDataCodec(): FixedSizeCodec<
  CloseOptionAccountInstructionDataArgs,
  CloseOptionAccountInstructionData
> {
  return combineCodec(
    getCloseOptionAccountInstructionDataEncoder(),
    getCloseOptionAccountInstructionDataDecoder()
  );
}

export type CloseOptionAccountAsyncInput<
  TAccountSigner extends string = string,
  TAccountCreator extends string = string,
  TAccountMarket extends string = string,
  TAccountOption extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  creator: Address<TAccountCreator>;
  market: Address<TAccountMarket>;
  option?: Address<TAccountOption>;
  systemProgram?: Address<TAccountSystemProgram>;
  optionId: CloseOptionAccountInstructionDataArgs['optionId'];
};

export async function getCloseOptionAccountInstructionAsync<
  TAccountSigner extends string,
  TAccountCreator extends string,
  TAccountMarket extends string,
  TAccountOption extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof OPPORTUNITY_MARKET_PROGRAM_ADDRESS,
>(
  input: CloseOptionAccountAsyncInput<
    TAccountSigner,
    TAccountCreator,
    TAccountMarket,
    TAccountOption,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  CloseOptionAccountInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountCreator,
    TAccountMarket,
    TAccountOption,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? OPPORTUNITY_MARKET_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    creator: { value: input.creator ?? null, isWritable: true },
    market: { value: input.market ?? null, isWritable: true },
    option: { value: input.option ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.option.value) {
    accounts.option.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([111, 112, 116, 105, 111, 110])
        ),
        getAddressEncoder().encode(expectAddress(accounts.market.value)),
        getU64Encoder().encode(expectSome(args.optionId)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.creator),
      getAccountMeta(accounts.market),
      getAccountMeta(accounts.option),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCloseOptionAccountInstructionDataEncoder().encode(
      args as CloseOptionAccountInstructionDataArgs
    ),
    programAddress,
  } as CloseOptionAccountInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountCreator,
    TAccountMarket,
    TAccountOption,
    TAccountSystemProgram
  >);
}

export type CloseOptionAccountInput<
  TAccountSigner extends string = string,
  TAccountCreator extends string = string,
  TAccountMarket extends string = string,
  TAccountOption extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  creator: Address<TAccountCreator>;
  market: Address<TAccountMarket>;
  option: Address<TAccountOption>;
  systemProgram?: Address<TAccountSystemProgram>;
  optionId: CloseOptionAccountInstructionDataArgs['optionId'];
};

export function getCloseOptionAccountInstruction<
  TAccountSigner extends string,
  TAccountCreator extends string,
  TAccountMarket extends string,
  TAccountOption extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof OPPORTUNITY_MARKET_PROGRAM_ADDRESS,
>(
  input: CloseOptionAccountInput<
    TAccountSigner,
    TAccountCreator,
    TAccountMarket,
    TAccountOption,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): CloseOptionAccountInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountCreator,
  TAccountMarket,
  TAccountOption,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? OPPORTUNITY_MARKET_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    creator: { value: input.creator ?? null, isWritable: true },
    market: { value: input.market ?? null, isWritable: true },
    option: { value: input.option ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.creator),
      getAccountMeta(accounts.market),
      getAccountMeta(accounts.option),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCloseOptionAccountInstructionDataEncoder().encode(
      args as CloseOptionAccountInstructionDataArgs
    ),
    programAddress,
  } as CloseOptionAccountInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountCreator,
    TAccountMarket,
    TAccountOption,
    TAccountSystemProgram
  >);
}

export type ParsedCloseOptionAccountInstruction<
  TProgram extends string = typeof OPPORTUNITY_MARKET_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    creator: TAccountMetas[1];
    market: TAccountMetas[2];
    option: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: CloseOptionAccountInstructionData;
};

export function parseCloseOptionAccountInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedCloseOptionAccountInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      creator: getNextAccount(),
      market: getNextAccount(),
      option: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCloseOptionAccountInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
