/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { OPPORTUNITY_MARKET_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const SET_WINNING_OPTION_DISCRIMINATOR = new Uint8Array([
  156, 73, 113, 33, 170, 115, 163, 206,
]);

export function getSetWinningOptionDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_WINNING_OPTION_DISCRIMINATOR
  );
}

export type SetWinningOptionInstruction<
  TProgram extends string = typeof OPPORTUNITY_MARKET_PROGRAM_ADDRESS,
  TAccountMarketAuthority extends string | AccountMeta<string> = string,
  TAccountMarket extends string | AccountMeta<string> = string,
  TAccountOption extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountMarketAuthority extends string
        ? ReadonlySignerAccount<TAccountMarketAuthority> &
            AccountSignerMeta<TAccountMarketAuthority>
        : TAccountMarketAuthority,
      TAccountMarket extends string
        ? WritableAccount<TAccountMarket>
        : TAccountMarket,
      TAccountOption extends string
        ? WritableAccount<TAccountOption>
        : TAccountOption,
      ...TRemainingAccounts,
    ]
  >;

export type SetWinningOptionInstructionData = {
  discriminator: ReadonlyUint8Array;
  optionId: bigint;
  rewardPercentage: number;
};

export type SetWinningOptionInstructionDataArgs = {
  optionId: number | bigint;
  rewardPercentage: number;
};

export function getSetWinningOptionInstructionDataEncoder(): FixedSizeEncoder<SetWinningOptionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['optionId', getU64Encoder()],
      ['rewardPercentage', getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_WINNING_OPTION_DISCRIMINATOR })
  );
}

export function getSetWinningOptionInstructionDataDecoder(): FixedSizeDecoder<SetWinningOptionInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['optionId', getU64Decoder()],
    ['rewardPercentage', getU8Decoder()],
  ]);
}

export function getSetWinningOptionInstructionDataCodec(): FixedSizeCodec<
  SetWinningOptionInstructionDataArgs,
  SetWinningOptionInstructionData
> {
  return combineCodec(
    getSetWinningOptionInstructionDataEncoder(),
    getSetWinningOptionInstructionDataDecoder()
  );
}

export type SetWinningOptionAsyncInput<
  TAccountMarketAuthority extends string = string,
  TAccountMarket extends string = string,
  TAccountOption extends string = string,
> = {
  marketAuthority: TransactionSigner<TAccountMarketAuthority>;
  market: Address<TAccountMarket>;
  option?: Address<TAccountOption>;
  optionId: SetWinningOptionInstructionDataArgs['optionId'];
  rewardPercentage: SetWinningOptionInstructionDataArgs['rewardPercentage'];
};

export async function getSetWinningOptionInstructionAsync<
  TAccountMarketAuthority extends string,
  TAccountMarket extends string,
  TAccountOption extends string,
  TProgramAddress extends Address = typeof OPPORTUNITY_MARKET_PROGRAM_ADDRESS,
>(
  input: SetWinningOptionAsyncInput<
    TAccountMarketAuthority,
    TAccountMarket,
    TAccountOption
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  SetWinningOptionInstruction<
    TProgramAddress,
    TAccountMarketAuthority,
    TAccountMarket,
    TAccountOption
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? OPPORTUNITY_MARKET_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    marketAuthority: {
      value: input.marketAuthority ?? null,
      isWritable: false,
    },
    market: { value: input.market ?? null, isWritable: true },
    option: { value: input.option ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.option.value) {
    accounts.option.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([111, 112, 116, 105, 111, 110])
        ),
        getAddressEncoder().encode(expectAddress(accounts.market.value)),
        getU64Encoder().encode(expectSome(args.optionId)),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.marketAuthority),
      getAccountMeta(accounts.market),
      getAccountMeta(accounts.option),
    ],
    data: getSetWinningOptionInstructionDataEncoder().encode(
      args as SetWinningOptionInstructionDataArgs
    ),
    programAddress,
  } as SetWinningOptionInstruction<
    TProgramAddress,
    TAccountMarketAuthority,
    TAccountMarket,
    TAccountOption
  >);
}

export type SetWinningOptionInput<
  TAccountMarketAuthority extends string = string,
  TAccountMarket extends string = string,
  TAccountOption extends string = string,
> = {
  marketAuthority: TransactionSigner<TAccountMarketAuthority>;
  market: Address<TAccountMarket>;
  option: Address<TAccountOption>;
  optionId: SetWinningOptionInstructionDataArgs['optionId'];
  rewardPercentage: SetWinningOptionInstructionDataArgs['rewardPercentage'];
};

export function getSetWinningOptionInstruction<
  TAccountMarketAuthority extends string,
  TAccountMarket extends string,
  TAccountOption extends string,
  TProgramAddress extends Address = typeof OPPORTUNITY_MARKET_PROGRAM_ADDRESS,
>(
  input: SetWinningOptionInput<
    TAccountMarketAuthority,
    TAccountMarket,
    TAccountOption
  >,
  config?: { programAddress?: TProgramAddress }
): SetWinningOptionInstruction<
  TProgramAddress,
  TAccountMarketAuthority,
  TAccountMarket,
  TAccountOption
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? OPPORTUNITY_MARKET_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    marketAuthority: {
      value: input.marketAuthority ?? null,
      isWritable: false,
    },
    market: { value: input.market ?? null, isWritable: true },
    option: { value: input.option ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.marketAuthority),
      getAccountMeta(accounts.market),
      getAccountMeta(accounts.option),
    ],
    data: getSetWinningOptionInstructionDataEncoder().encode(
      args as SetWinningOptionInstructionDataArgs
    ),
    programAddress,
  } as SetWinningOptionInstruction<
    TProgramAddress,
    TAccountMarketAuthority,
    TAccountMarket,
    TAccountOption
  >);
}

export type ParsedSetWinningOptionInstruction<
  TProgram extends string = typeof OPPORTUNITY_MARKET_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    marketAuthority: TAccountMetas[0];
    market: TAccountMetas[1];
    option: TAccountMetas[2];
  };
  data: SetWinningOptionInstructionData;
};

export function parseSetWinningOptionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedSetWinningOptionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      marketAuthority: getNextAccount(),
      market: getNextAccount(),
      option: getNextAccount(),
    },
    data: getSetWinningOptionInstructionDataDecoder().decode(instruction.data),
  };
}
